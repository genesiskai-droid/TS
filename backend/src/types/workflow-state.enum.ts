export enum WorkflowState {
  Draft = 'draft',
  Submitted = 'submitted',
  InReview = 'in_review',
  Approved = 'approved',
  Rejected = 'rejected',
  InProgress = 'in_progress',
  Completed = 'completed',
  Cancelled = 'cancelled',
}
